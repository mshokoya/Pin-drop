console.log(global.ApolloTestServer)

describe('Auth resolver integration test', () => {
  it.skip('', () => {
    expect(1).toBe(1);
  });

  it.skip('should work 1', () => {
    expect(1).toBe(1);
  });
})